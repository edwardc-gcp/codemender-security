# CodeMender Configuration Reference (`.codemender/config.yaml`)

This document reflects the exact, ground-truth configuration schema generated by `cm init` in **CodeMender 0.8.0+**, along with the **Surgical Tuning Playbook** for troubleshooting scan coverage and performance.

---

## 1. Ground-Truth `config.yaml` Schema (`cm init` 0.8.0)

```yaml
# CodeMender CLI configuration.
# The server endpoint is baked in at build time (see build profiles).
# Override it here only if you need a different endpoint.

server: {}

# Customer-supplied team identifier sent as the X-CodeMender-Team-Id
# telemetry header. Overridden by --team-id flag or $CM_TEAM_ID env var.
# Max 63 chars, must match [A-Za-z0-9._-]. Omit to skip the header.
# team_id: "acme-security"

scan:
  extensions:
    include: [".py", ".java", ".go", ".js", ".ts", ".c", ".cc", ".cpp", ".h", ".rb", ".php"]
    exclude: [".min.js", ".generated.go", ".pb.go"]
  exclude_dirs: ["node_modules"]
  max_file_size_kb: 500
  incremental: true

# Restrict agent file access to these directories (all commands).
# When empty, the scan target directory is used as the boundary.
# project_paths:
#   - "/path/to/project"
#   - "/path/to/shared-lib"

output:
  format: table   # "table", "json", "sarif"

tools:
  confirm_commands: true   # Prompt Y/N before running shell commands (default: true)
  confirm_writes: true     # Prompt Y/N before writing/modifying files (default: true)
  # cleanup_candidate_branches: true # Auto-delete temporary candidate branches on completion (default: true)
  # Use --yes flag on find/fix/session resume to skip all confirmations

vcs:
  type: ""               # VCS type: "git", "mercurial", "custom", or "" (auto-detect)
  commands:
    reset: ""            # Shell command to reset codebase (auto-populated for known VCS)
    diff: ""             # Shell command to show changes (auto-populated for known VCS)
    status: ""           # Shell command to list modified files (auto-populated for known VCS)
    stage: ""            # Shell command to stage changes (auto-populated for known VCS)
  # Known VCS types (git, mercurial) auto-populate commands.
  # For custom VCS, you MUST set the commands above.
  # Examples:
  #   type: git           # uses: git checkout HEAD -- . && git clean -fd
  #   type: mercurial     # uses: hg revert --all --no-backup && hg purge
  #   type: custom
  #   commands:
  #     reset: "./scripts/reset.sh"
  #     diff: "diff -rq baseline/ current/"
  #     stage: "git add -A"

build:
  command: ""              # Shell command to build/verify changes from the agent
  # Examples:
  #   Make:       "make build && make test"
  #   Go:         "go build ./... && go test ./..."
  #   Custom:     "./scripts/verify.sh"
```

---

## 1b. Additional Official Public-Doc Blocks (`sandbox` & `security`)

While `cm init` (v0.8.0) generates the concise template in Section 1, the official CodeMender documentation also defines optional `sandbox` and `security` blocks that can be added to `.codemender/config.yaml`:

```yaml
sandbox:
  enabled: true
  network:
    # "permissive-closed" (default: blocks network/socket access during tool runs)
    # "permissive-open"   (allows local socket & network access for builds/tests while keeping filesystem sandboxing)
    profile: "permissive-closed"

security:
  # Mount sensitive host credential paths as read-only inside the sandbox
  protected_files:
    - "~/.ssh/*"
    - "~/.aws/*"
    - "~/.config/gcloud/*"
```

> **Schema Naming Note (Web Docs vs. `cm` v0.8.0)**: Some pages in the Google Cloud web documentation refer to `human_confirmation` and `exclude_paths`. In the `cm` v0.8.0 CLI schema generated by `cm init`, those fields correspond to `tools.confirm_commands` / `tools.confirm_writes` and `scan.exclude_dirs` respectively. Always use the `cm init` v0.8.0 key names shown in Section 1.

---

## 2. Key Parameters Breakdown

### `scan`
* `scan.extensions.include`: Whitelist of file extensions for AST & taint analysis. Defaults strictly to 11 suffixes (`[".py", ".java", ".go", ".js", ".ts", ".c", ".cc", ".cpp", ".h", ".rb", ".php"]`) to keep scan latency and token usage minimal.
* `scan.extensions.exclude`: Suffixes skipped during scanning (`[".min.js", ".generated.go", ".pb.go"]`).
* `scan.exclude_dirs`: Directories skipped during scanning (defaults only to `["node_modules"]`).
* `scan.max_file_size_kb`: File size threshold (default `500` KB) to skip large binary/data assets.
* `scan.incremental`: **(Default: `true`)** When enabled, CodeMender caches AST analysis in `.codemender/state.db` and performs incremental differential scans on modified files automatically.

### `tools` (Traceability Rule)
* `tools.confirm_commands` & `tools.confirm_writes`: Default to `true`.
* **Best Practice**: **Keep both set to `true` in `.codemender/config.yaml`.** Do not mutate `config.yaml` to disable confirmations; instead, explicitly pass `--bypass-warning -y` on the CLI command line so non-interactive execution remains visible and auditable in shell logs.

### `vcs`
* `vcs.type`: VCS provider. Defaults to `""` for automatic detection (`git` or `mercurial`).
* **VCS Safety Note**: Notice that `type: git` uses `git checkout HEAD -- . && git clean -fd` for resets!
  1. Always register `.codemender/`, `.cm_project`, and `.exploit/` in `.git/info/exclude` so `git clean -fd` never deletes `.codemender/state.db` mid-session.
  2. Always run `git stash push -u -m "cm-pre-fix-backup-$(date +%s)"` before `cm fix`, and **always restore the user's files (`git stash pop`) after committing the security patches**.

### `build`
* `build.command`: Verification command executed by `cm fix` inside the sandbox (`exebox`).
* **Tiered Build & Sandbox Resolution**:
  1. Pre-install dependencies on the host and probe the build binary dynamically (`command -v pytest`, `command -v python3`, `command -v node`).
  2. If `build.command` or `cm verify` fails solely because the sandbox blocks network/socket connections (`permissive-closed`), set `sandbox.network.profile: "permissive-open"` in `.codemender/config.yaml` (keeping filesystem isolation active).
  3. If no test suite exists (`npm test` returns 1) or macOS `exebox` blocks host toolchain execution paths (`~/.nvm`, `/opt/homebrew`, `~/.pyenv`), set `build.command: "true"` in `.codemender/config.yaml` and enforce the real build check via `OUTER_BUILD_CMD` in the outer shell before `git commit`.

---

## 3. Surgical Tuning Playbook (Troubleshooting Coverage vs. Performance)

> **Golden Rule**: Never blindly add every possible extension to `scan.extensions.include` upfront—every extra extension increases file count, scan latency, and token consumption. Only apply surgical adjustments to `.codemender/config.yaml` based on the specific project stack or observed symptom:

| Observed Symptom / Scenario | Root Cause in `config.yaml` | Surgical `config.yaml` Adjustment |
| :--- | :--- | :--- |
| **Next.js / React / Vite app returns `0 findings` or misses Server Action / JSX bugs** | `.tsx`, `.jsx`, `.mjs`, `.vue`, `.svelte` are not in default `scan.extensions.include` | Append **only** the suffixes present in `src/` or `app/` (e.g., add `".tsx", ".jsx"` to `scan.extensions.include`) |
| **Rust / Kotlin / Swift / C# / C++ (`.hpp`) project files are skipped** | `.rs`, `.kt`, `.swift`, `.cs`, `.hpp`, `.hxx` are not in default `scan.extensions.include` | Append **only** the specific target language suffixes present in the repo (e.g., `".cs"`, `".rs"`, or `".hpp"`) to `scan.extensions.include` |
| **User asks to audit IaC, deployment configs, CI scripts, or hardcoded config secrets** | `.yaml`, `.yml`, `.tf`, `.sh`, `.json`, `.xml`, `.properties` are excluded from default `scan.extensions.include` | Surgically append the relevant config/script suffix (e.g., `".yaml", ".tf", ".sh"`) while keeping lockfiles/bundles in `scan.extensions.exclude` and `scan.exclude_dirs` |
| **Scan is unusually slow or consumes excessive tokens on Python / Next.js / Go / Rust / .NET repos** | `scan.exclude_dirs` only excludes `["node_modules"]` by default, causing `cm` to scan `.venv/` (`site-packages`) or `.next/` / `dist/` / `target/` / `bin/` bundles | Append existing build/virtualenv folders in the workspace to `scan.exclude_dirs`: e.g. `[".venv", "venv", "__pycache__", ".next", "dist", "build", "vendor", "target", "bin", "obj"]` |
| **Known vulnerable controller or monolithic file is skipped** | File size exceeds `scan.max_file_size_kb: 500` | Check file size (`ls -lh <file>`); if >500 KB, raise `scan.max_file_size_kb` (e.g., to `1500`) for the audit |
| **False positive in a monorepo sub-package because shared sanitizer in `../shared` wasn't read** | `project_paths` defaults to `[]` (using the scan target directory as the least-privilege boundary) | Add the shared package directory to `project_paths: ["/abs/path/to/app", "/abs/path/to/shared"]` |
| **Subdirectory scan (`cm find ./src`) causes `cm verify` / `cm fix` to miss root `Cargo.toml` / `package.json` or creates a nested `<subdir>/.git`** | When `project_paths` is empty (`[]`, default for monorepo isolation), `cm __worker` locks `--workspace` and `--allowed-roots` to `./src` and runs `git init` inside `./src` if `.git` is absent | **Keep `project_paths` empty on Turn 1.** If a subdirectory scan in a single-repo project triggers root-file access errors or creates `<subdir>/.git`, reactively set `project_paths: ["/abs/path/to/project_root"]` and remove `<subdir>/.git` & `<subdir>/.gitignore` |
| **`cm verify` or `cm fix` stalls >60s (15+ steps in `.codemender/logs/`) running `which mono` or `find / -name cargo`** | Cloud agent invokes `run_command` to locate compilers blocked by macOS `exebox` (`/opt/homebrew`, `~/.cargo`, `~/.nvm`) | **Keep native `-c` on Turn 1** (preserving in-loop compiler self-correction). Only if `.codemender/logs/` shows compiler-search loops, re-run with `-c "<guidance>. IMPORTANT: Do NOT call run_command in sandbox; apply fix via search_and_replace and rely on outer build gate"` + `--no-cache --bypass-warning -y` |
